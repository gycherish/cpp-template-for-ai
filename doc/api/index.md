# API 参考

本页内容由 Doxygen 从 `include/` 与 `src/` 中的 `///` 注释自动抽取，经 Breathe
注入到本站点。`xmake doc` / `xmake doc-api` 会在构建前先运行 Doxygen。

待项目有源码后，取消下方注释即可列出全部 API；也可用 `doxygenclass`、
`doxygenfunction` 等指令精确嵌入单个符号。

````{note}
当前仓库尚无源码，故 API 列表为空。示例用法：

```text
```{doxygenindex}
:project: cpp-template-for-ai
```
```
````

<!-- 源码就绪后启用：
```{doxygenindex}
:project: cpp-template-for-ai
```
-->
